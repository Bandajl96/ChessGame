class BoardSquares{
  constructor(gameWidth, gameHeight, chessArray){
    this.width = 95;
    this.height = 95;
    
    this.position = {
      x: 

    }

  }







draw(ctx) {
  for (var rows = 0; rows < x; rows++) {
    for (var columns = 0; columns < x; columns++) {
      ctx.append("<div class='grid'></div>");
    };
  };
  $(".grid").width(960/x);
  $(".grid").height(960/x);
}


